Config = {}

-------
-- Doors
-------

-- Radius of the player's door zone, in which all doors will be stored and looped through (default 50.0)
Config.Radius = 50.0

-- Distance from a door which a player must be within to lock/unlock (default 1.75)
Config.DoorActivationRange = 1.75

-- Distance that one door must be from another to be considered a secondary door and be switchable (default 2.25)
Config.DoorSecondaryRange = 2.25

-- Distance that the player must maintain with a triggered door to keep control (must be higher than DoorActivationRange) (default 2.0)
Config.DoorDeactivateRange = 2.0
